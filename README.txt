Matt Shore
Marc Leef

Submitted Source Files:
shell.c         --  Shell source code
tokenizer.c     --  Tokenizer source code

Submitted Header Files:
tokenizer.h     --  Tokenizer header file

Extra Credit Answers:
- The disadvantages of synchronous job control



- I can tell the difference between the library function and the system call by the number to the right of the function's/call's name. The 2 represents the system calls. The 3 represents the library functions.

Compilation Instructions:
- In the hw2 directory use the command 'make' to automatically compile tokenizer.c and shell.c
- Then use the command 'make clean' to remove the executable file created from compiling 
